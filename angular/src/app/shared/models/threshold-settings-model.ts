export class ThresholdSettingsModel {
  upperEnabled: boolean = false;
  upperValue: number;
  upperColor: string;
  lowerEnabled: boolean = false;
  lowerValue: number;
  lowerColor: string;
  deviationColor: string;
}