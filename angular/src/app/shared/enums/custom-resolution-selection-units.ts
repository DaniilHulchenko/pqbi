// Order units only in ascending order, from smallest to biggest
export enum CustomResolutionUnits {
    MS = 'MS',
    SEC = 'SEC',
    MIN = 'MIN',
    HOUR = 'HOUR',
    DAY = 'DAY',
    WEEK = 'WEEK',
}